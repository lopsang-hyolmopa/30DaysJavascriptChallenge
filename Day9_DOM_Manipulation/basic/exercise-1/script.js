// Button Click to Change Text
// Create a button that, when clicked, changes the text of a paragraph from "Hello!" to "Goodbye!".
const para = document.querySelector('p')
const btn = document.querySelector('button')

btn.addEventListener('click', () => para.textContent = 'Goodbye!')

// Image Toggle
// Add an image to your webpage and a button labeled "Toggle Image." Clicking the button should hide the image if it’s visible or show it if it’s hidden.

// Color Picker
// Create three buttons labeled "Red," "Green," and "Blue." Clicking each button should change the background color of the page to the respective color.

